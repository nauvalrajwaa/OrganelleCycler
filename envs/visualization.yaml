name: visualization
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bandage      # <--- GANTI DARI bandage-ng KE bandage
  - imagemagick
  - graphviz     # Tambahan opsional untuk dependensi gambar