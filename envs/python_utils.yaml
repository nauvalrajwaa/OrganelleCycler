name: python_utils
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.9
  - blast             # PENTING: cleaner.py memerlukan 'blastn' agar berfungsi
  - biopython         # Opsional (Good practice): Berguna jika script solver dikembangkan nanti
  - pip